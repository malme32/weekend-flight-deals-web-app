{
  "name": "weekend-flight-deals-web-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static page listing cheap Ryanair Friday-to-Monday round-trip flight deals from Athens.",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test test/",
    "fetch": "node scripts/fetch-deals.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
